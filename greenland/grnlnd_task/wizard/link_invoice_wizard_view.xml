<odoo>
    <record id="view_link_invoice_wizard_form" model="ir.ui.view">
        <field name="name">link.invoice.wizard.form</field>
        <field name="model">link.invoice.wizard</field>
        <field name="arch" type="xml">
            <form string="Link to Existing Invoice">
                <sheet>
                    <group>
                        <field name="partner_id"/>
                        <field name="invoice_id" options="{'no_create': True}"/>
                        <field name="task_ids" widget="many2many_tags" invisible="1"/>
                    </group>
                </sheet>
                <footer>
                    <button name="action_link_invoice" string="Link Invoice" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_link_invoice_wizard" model="ir.actions.act_window">
        <field name="name">Link to Existing Invoice</field>
        <field name="res_model">link.invoice.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" eval="False"/>
        <field name="binding_view_types">list</field>
    </record>
</odoo>
